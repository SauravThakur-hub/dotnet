name: for dev

on:
  push:
    branches:
      - "dev"

  workflow_dispatch:

jobs:
  display_secrets:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          ref: dev

      - name: echo secrets
        run: echo "dev"